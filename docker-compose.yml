version: "3"

services:
  nuxt:
    container_name: nuxt
    image: registry.gitlab.com/rtexel/fox:latest
    restart: always
    ports:
      - "3000:3000"
    command:
      "npm run start"

  nginx:
    image: nginx:1.15
    container_name: ngx
    ports:
      - "80:80"
    volumes:
      - ./nginx:/etc/nginx/conf.d
    depends_on:
      - nuxt